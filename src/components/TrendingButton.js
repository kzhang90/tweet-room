import React from 'react';

export default class TrendingButtong extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <button>Get Tags</button>
    )
  }
}